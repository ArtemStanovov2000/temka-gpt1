export const attention = (embeddings: number[][]) => {
    // Нормализация эмбеддингов
    // умножение на матрицу W_q
    // умножение на матрицу W_k
    // умножение на матрицу W_v

    // Вычисляем внимание умножая матрицу W_q на W_k
    // Умножаем на матрицу W_v возвращаем исходную размерность

    // Нормализация
    // Полносвязный слой
}